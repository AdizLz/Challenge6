<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ofertas Recibidas</title>
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px 0;
        }
        .offers-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            padding: 40px;
            max-width: 1200px;
            margin: 0 auto;
        }
        .page-title {
            color: #667eea;
            font-weight: 700;
            text-align: center;
            margin-bottom: 40px;
        }
        .offer-card {
            background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            border-left: 5px solid #667eea;
            transition: all 0.3s ease;
        }
        .offer-card:hover {
            transform: translateX(10px);
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
        }
        .offer-name {
            font-size: 1.3rem;
            font-weight: 600;
            color: #2d3748;
        }
        .offer-email {
            color: #718096;
            font-size: 0.95rem;
        }
        .offer-amount {
            font-size: 1.8rem;
            font-weight: 700;
            color: #48bb78;
        }
        .offer-item {
            background: white;
            padding: 10px 20px;
            border-radius: 10px;
            display: inline-block;
            margin-top: 10px;
        }
        .btn-back {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
        }
        .btn-back:hover {
            color: #764ba2;
            gap: 12px;
        }
        .stats-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            margin-bottom: 30px;
        }
        .stats-number {
            font-size: 3rem;
            font-weight: 700;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="offers-container">
            <!-- Header -->
            <a href="/items" class="btn-back mb-4">
                <i class="bi bi-arrow-left-circle"></i> Volver a la lista
            </a>

            <h1 class="page-title">
                <i class="bi bi-clipboard-check"></i> Ofertas Recibidas
            </h1>

            <!-- Estadísticas -->
            <div class="row mb-4">
                <div class="col-md-4">
                    <div class="stats-card">
                        <i class="bi bi-hand-thumbs-up display-4"></i>
                        <div class="stats-number">{{offers.size}}</div>
                        <div>Ofertas Totales</div>
                    </div>
                </div>
            </div>

            <!-- Lista de Ofertas -->
            {{#offers}}
            <div class="offer-card">
                <div class="row align-items-center">
                    <div class="col-md-4">
                        <div class="offer-name">
                            <i class="bi bi-person-circle"></i> {{name}}
                        </div>
                        <div class="offer-email">
                            <i class="bi bi-envelope"></i> {{email}}
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="offer-item">
                            <i class="bi bi-gem text-warning"></i>
                            <a href="/items/{{id}}" class="text-decoration-none">{{itemName}}</a>
                        </div>
                    </div>
                    <div class="col-md-4 text-end">
                        <div class="offer-amount">
                            <i class="bi bi-cash-stack"></i> ${{amount}}
                        </div>
                        <small class="text-muted">USD</small>
                    </div>
                </div>
            </div>
            {{/offers}}

            {{^offers}}
            <div class="alert alert-info text-center" role="alert">
                <i class="bi bi-info-circle display-1"></i>
                <h3 class="mt-3">No hay ofertas todavía</h3>
                <p>Sé el primero en hacer una oferta por alguno de nuestros artículos exclusivos.</p>
                <a href="/items" class="btn btn-primary mt-3">
                    <i class="bi bi-arrow-right"></i> Ver artículos disponibles
                </a>
            </div>
            {{/offers}}
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>